# Master-Thesis
The repositry contains python code and dataset utilized in Mingrou Wei's Master Theisis

File folder 'back_translation & sentiment' should be renamed as 'back_translation & semantic_analysis"

'GPT-4o' and 'Gemini-2.5-flash' contains each part of LLM MT test with different aspect experiment, each folder in them can correspond to thesis section under each model's evaluation.

